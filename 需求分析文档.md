# 校园二手商品交易系统需求分析文档

## 1. 项目概述

### 1.1 项目背景
随着高校学生数量的增加和消费水平的提高，校园内产生了大量的闲置物品。传统的线下交易方式存在信息不对称、交易效率低、缺乏安全保障等问题。为了促进校园内资源的循环利用，提高交易效率，保障交易安全，特开发本校园二手商品交易系统。

### 1.2 项目目标
- 为校园用户提供便捷、安全的二手商品交易平台
- 实现商品信息的统一管理和展示
- 提供完整的交易流程管理功能
- 支持用户收藏、搜索、分类浏览等便捷功能
- 为管理员提供用户和商品管理功能

### 1.3 项目范围
本系统主要面向校园内的学生和教师，提供二手商品的发布、浏览、搜索、交易等功能。系统采用前后端分离架构，前端使用React开发，后端使用Flask框架。

## 2. 用户角色与权限

### 2.1 用户角色
系统包含以下三种用户角色：

1. **学生（student）**
   - 可以注册、登录系统
   - 可以发布、编辑、删除自己的商品
   - 可以浏览、搜索、收藏商品
   - 可以发起交易请求、管理自己的交易
   - 可以查看和管理个人资料
   - 可以查看订单记录

2. **教师（teacher）**
   - 拥有与学生相同的所有权限
   - 可以发布、编辑、删除自己的商品
   - 可以浏览、搜索、收藏商品
   - 可以发起交易请求、管理自己的交易

3. **管理员（admin）**
   - 拥有所有普通用户的权限
   - 可以查看和管理所有用户（启用/禁用用户）
   - 可以查看和管理所有商品（禁用/启用商品，需提供禁用理由）
   - 可以查看所有交易记录
   - 不能禁用自己的账号
   - 不能禁用其他管理员账号

### 2.2 权限说明
- 普通用户只能查看可交易且未被禁用的商品
- 用户只能编辑和删除自己发布的商品
- 管理员可以编辑和删除任何商品
- 用户只能查看自己相关的交易记录
- 管理员可以查看所有交易记录

## 3. 功能需求

### 3.1 用户管理子系统

#### 3.1.1 用户注册
**功能描述：** 新用户可以通过注册功能创建账号

**功能要求：**
- 用户需要提供用户名、密码、邮箱（必填）
- 用户可以选择提供手机号、头像（可选）
- 系统需要验证用户名和邮箱的唯一性
- 默认用户类型为学生（student）
- 只有管理员可以创建管理员账号
- 注册成功后返回用户ID

**输入：**
- 用户名（必填，唯一）
- 密码（必填）
- 邮箱（必填，唯一）
- 手机号（可选）
- 头像（可选）
- 用户类型（可选，默认为student）

**输出：**
- 注册成功消息
- 用户ID

**异常处理：**
- 用户名已存在：返回错误提示
- 邮箱已被注册：返回错误提示
- 必填字段为空：返回错误提示
- 非管理员尝试创建管理员账号：返回权限错误

#### 3.1.2 用户登录
**功能描述：** 已注册用户可以通过用户名和密码登录系统

**功能要求：**
- 用户需要提供用户名、密码
- 用户需要选择登录类型（学生/教师 或 管理员）
- 系统验证用户类型是否匹配
- 系统检查用户是否处于活跃状态
- 登录成功后返回用户信息（用户ID、用户名、邮箱、用户类型）

**输入：**
- 用户名
- 密码
- 用户类型（student/teacher 或 admin）

**输出：**
- 登录成功消息
- 用户ID
- 用户名
- 邮箱
- 用户类型

**异常处理：**
- 用户名或密码错误：返回错误提示
- 用户类型不匹配：返回错误提示
- 用户账号被禁用：返回错误提示

#### 3.1.3 用户个人信息管理
**功能描述：** 用户可以查看和修改自己的个人信息

**功能要求：**
- 用户可以查看个人信息（用户名、邮箱、手机号、头像、用户类型、注册时间）
- 用户可以修改用户名、邮箱、手机号、头像、密码
- 修改用户名和邮箱时需要验证唯一性
- 需要登录才能访问

**输入：**
- 用户名（可选）
- 邮箱（可选）
- 手机号（可选）
- 头像（可选）
- 密码（可选）

**输出：**
- 更新成功消息

**异常处理：**
- 未登录：返回401错误
- 用户名已存在：返回错误提示
- 邮箱已被注册：返回错误提示

#### 3.1.4 用户列表管理（管理员）
**功能描述：** 管理员可以查看和管理所有用户

**功能要求：**
- 支持分页显示用户列表
- 支持按用户名或邮箱搜索
- 支持按用户类型筛选（学生/教师/管理员）
- 显示用户信息：用户名、邮箱、手机号、用户类型、状态、注册时间
- 管理员可以启用/禁用用户（不能禁用自己的账号和其他管理员账号）

**输入：**
- 页码（可选，默认1）
- 每页数量（可选，默认10）
- 搜索关键词（可选）
- 用户类型（可选）

**输出：**
- 用户列表
- 总记录数
- 总页数
- 当前页码

**异常处理：**
- 非管理员访问：返回403错误
- 尝试禁用自己的账号：返回错误提示
- 尝试禁用其他管理员账号：返回错误提示

### 3.2 商品管理子系统

#### 3.2.1 商品发布
**功能描述：** 用户可以发布二手商品信息

**功能要求：**
- 用户需要提供商品标题、描述、价格、分类（必填）
- 用户可以上传商品图片（支持多张图片）
- 用户可以选择提供位置、品牌、新旧程度等信息（可选）
- 商品状态默认为"可交易"（available）
- 商品默认启用状态（is_active = true）
- 需要登录才能发布商品

**输入：**
- 标题（必填）
- 描述（必填）
- 价格（必填，浮点数）
- 分类（必填）
- 图片（可选，支持多张）
- 位置（可选）
- 品牌（可选）
- 新旧程度（可选：new, like_new, good, fair, poor）

**输出：**
- 创建成功消息
- 商品ID

**异常处理：**
- 未登录：返回401错误
- 必填字段为空：返回错误提示

#### 3.2.2 商品列表浏览
**功能描述：** 用户可以浏览商品列表

**功能要求：**
- 支持分页显示商品列表
- 支持按分类筛选
- 支持按关键词搜索（标题、描述）
- 支持按状态筛选（可交易/交易中/已售出）
- 普通用户默认只显示可交易且未被禁用的商品
- 用户可以查看"我的商品"（显示自己发布的所有商品，包括被禁用的）
- 管理员可以查看所有商品（包括被禁用的）
- 显示商品信息：标题、价格、图片、分类、状态、卖家信息、浏览量、收藏数等

**输入：**
- 页码（可选，默认1）
- 每页数量（可选，默认10）
- 分类（可选）
- 搜索关键词（可选）
- 状态（可选）
- 是否只看我的商品（可选）

**输出：**
- 商品列表
- 总记录数
- 总页数
- 当前页码

**异常处理：**
- 无异常情况

#### 3.2.3 商品详情查看
**功能描述：** 用户可以查看商品的详细信息

**功能要求：**
- 显示商品的完整信息（标题、描述、价格、图片、分类、状态、卖家信息、位置、品牌、新旧程度、浏览量、收藏数等）
- 普通用户只能查看可交易且未被禁用的商品（自己的商品除外）
- 管理员可以查看所有商品
- 查看详情时自动增加浏览量
- 显示相关推荐商品（同分类的其他商品）
- 需要登录才能查看详情

**输入：**
- 商品ID

**输出：**
- 商品详细信息
- 卖家信息
- 相关推荐商品列表

**异常处理：**
- 未登录：返回401错误
- 商品不存在：返回404错误
- 商品已被禁用且不是自己的商品：返回404错误

#### 3.2.4 商品编辑
**功能描述：** 用户可以编辑自己发布的商品

**功能要求：**
- 用户可以修改商品的所有信息（标题、描述、价格、分类、图片、位置、品牌、新旧程度、状态等）
- 只能编辑自己发布的商品（管理员可以编辑任何商品）
- 需要登录才能编辑

**输入：**
- 商品ID
- 要修改的字段（可选）

**输出：**
- 更新成功消息

**异常处理：**
- 未登录：返回401错误
- 商品不存在：返回404错误
- 没有权限编辑：返回403错误

#### 3.2.5 商品删除
**功能描述：** 用户可以删除自己发布的商品

**功能要求：**
- 只能删除自己发布的商品（管理员可以删除任何商品）
- 如果商品有未完成的交易，不能删除
- 删除商品时同时删除相关的收藏记录
- 删除商品时删除商品图片文件
- 已完成或已取消的交易记录会一并删除
- 需要登录才能删除

**输入：**
- 商品ID

**输出：**
- 删除成功消息

**异常处理：**
- 未登录：返回401错误
- 商品不存在：返回404错误
- 没有权限删除：返回403错误
- 商品有未完成的交易：返回错误提示

#### 3.2.6 商品管理（管理员）
**功能描述：** 管理员可以禁用/启用商品

**功能要求：**
- 管理员可以禁用商品（需要提供禁用理由）
- 管理员可以启用被禁用的商品
- 禁用商品时，商品对普通用户不可见
- 禁用商品时，商品状态保持不变
- 只有管理员可以执行此操作

**输入：**
- 商品ID
- 禁用理由（禁用时必填）

**输出：**
- 操作成功消息
- 商品当前状态
- 禁用理由（如果被禁用）

**异常处理：**
- 非管理员访问：返回403错误
- 商品不存在：返回404错误
- 禁用时未提供理由：返回错误提示

### 3.3 交易管理子系统

#### 3.3.1 创建交易请求
**功能描述：** 买家可以对商品发起交易请求

**功能要求：**
- 买家选择商品后可以发起交易请求
- 不能购买自己的商品
- 商品必须是可交易状态且未被禁用
- 不能对同一商品重复发起待处理的交易请求
- 创建交易时，商品状态自动变为"交易中"（pending）
- 同时创建买家和卖家的订单记录
- 可以添加备注信息
- 可以设置最终成交价格（默认使用商品原价）
- 需要登录才能创建交易

**输入：**
- 商品ID（必填）
- 备注（可选）
- 最终价格（可选，默认使用商品原价）

**输出：**
- 创建成功消息
- 交易ID

**异常处理：**
- 未登录：返回401错误
- 商品不存在：返回404错误
- 商品已被禁用：返回错误提示
- 商品已售出或已被预订：返回错误提示
- 不能购买自己的商品：返回错误提示
- 已经申请过此商品的交易：返回错误提示

#### 3.3.2 交易列表查看
**功能描述：** 用户可以查看自己的交易列表

**功能要求：**
- 支持分页显示交易列表
- 支持按状态筛选（待处理/已接受/已完成/已取消）
- 支持按角色筛选（作为买家/作为卖家）
- 管理员可以查看所有交易
- 显示交易信息：商品信息、买家信息、卖家信息、状态、时间等

**输入：**
- 页码（可选，默认1）
- 每页数量（可选，默认10）
- 状态（可选）
- 角色（可选：buyer/seller）

**输出：**
- 交易列表
- 总记录数
- 总页数
- 当前页码

**异常处理：**
- 未登录：返回401错误

#### 3.3.3 交易详情查看
**功能描述：** 用户可以查看交易的详细信息

**功能要求：**
- 显示交易的完整信息（商品信息、买家信息、卖家信息、状态、时间、备注、最终价格等）
- 管理员可以查看所有交易
- 普通用户只能查看自己相关的交易
- 需要登录才能查看

**输入：**
- 交易ID

**输出：**
- 交易详细信息

**异常处理：**
- 未登录：返回401错误
- 交易不存在：返回404错误
- 没有权限查看：返回403错误

#### 3.3.4 接受交易
**功能描述：** 卖家可以接受买家的交易请求

**功能要求：**
- 只有卖家或管理员可以接受交易
- 交易状态必须是"待处理"（pending）
- 接受交易后，交易状态变为"已接受"（accepted）
- 接受交易时记录接受时间
- 相关的订单记录状态也会更新
- 需要登录才能接受交易

**输入：**
- 交易ID

**输出：**
- 接受成功消息

**异常处理：**
- 未登录：返回401错误
- 交易不存在：返回404错误
- 没有权限接受：返回403错误
- 交易状态不允许接受：返回错误提示

#### 3.3.5 完成交易
**功能描述：** 交易双方可以标记交易为已完成

**功能要求：**
- 交易状态必须是"已接受"（accepted）
- 交易双方或管理员可以完成交易
- 完成交易后，交易状态变为"已完成"（completed）
- 商品状态自动变为"已售出"（sold）
- 完成交易时记录完成时间
- 相关的订单记录状态也会更新
- 需要登录才能完成交易

**输入：**
- 交易ID

**输出：**
- 完成成功消息

**异常处理：**
- 未登录：返回401错误
- 交易不存在：返回404错误
- 没有权限完成：返回403错误
- 交易状态不允许完成：返回错误提示

#### 3.3.6 取消交易
**功能描述：** 买家可以取消交易请求

**功能要求：**
- 管理员可以取消任何交易
- 普通用户只能取消自己的待处理或已接受的交易
- 取消交易后，交易状态变为"已取消"（cancelled）
- 如果商品状态是"交易中"，则恢复为"可交易"（available）
- 相关的订单记录状态也会更新
- 需要登录才能取消交易

**输入：**
- 交易ID

**输出：**
- 取消成功消息

**异常处理：**
- 未登录：返回401错误
- 交易不存在：返回404错误
- 没有权限取消：返回403错误

### 3.4 收藏功能

#### 3.4.1 添加收藏
**功能描述：** 用户可以收藏感兴趣的商品

**功能要求：**
- 用户可以收藏任何可交易的商品
- 不能重复收藏同一个商品
- 需要登录才能收藏

**输入：**
- 商品ID

**输出：**
- 收藏成功消息

**异常处理：**
- 未登录：返回401错误
- 商品不存在：返回404错误
- 已收藏过该商品：返回错误提示

#### 3.4.2 取消收藏
**功能描述：** 用户可以取消已收藏的商品

**功能要求：**
- 用户可以取消自己收藏的商品
- 需要登录才能取消收藏

**输入：**
- 商品ID

**输出：**
- 取消收藏成功消息

**异常处理：**
- 未登录：返回401错误
- 未收藏该商品：返回404错误

#### 3.4.3 收藏列表查看
**功能描述：** 用户可以查看自己的收藏列表

**功能要求：**
- 显示用户收藏的所有商品
- 显示商品的基本信息（标题、价格、图片、分类、状态等）
- 需要登录才能查看

**输入：**
- 无

**输出：**
- 收藏商品列表
- 总数量

**异常处理：**
- 未登录：返回401错误

#### 3.4.4 检查收藏状态
**功能描述：** 检查商品是否已被当前用户收藏

**功能要求：**
- 返回商品是否已被收藏的布尔值
- 未登录时返回false

**输入：**
- 商品ID

**输出：**
- 是否已收藏（布尔值）

**异常处理：**
- 无异常情况

### 3.5 联系卖家功能

#### 3.5.1 联系卖家
**功能描述：** 买家可以联系卖家获取更多信息

**功能要求：**
- 买家可以联系卖家
- 不能联系自己的商品
- 商品必须未被禁用
- 卖家账号必须处于活跃状态
- 可以添加留言信息
- 如果之前已经联系过，会更新联系记录
- 返回卖家联系方式（邮箱、手机号）
- 需要登录才能联系卖家

**输入：**
- 商品ID（必填）
- 留言信息（可选）

**输出：**
- 联系成功消息
- 卖家信息（用户名、邮箱、手机号）
- 商品信息
- 联系请求信息

**异常处理：**
- 未登录：返回401错误
- 商品不存在：返回404错误
- 不能联系自己的商品：返回错误提示
- 商品已被禁用：返回错误提示
- 卖家账号已被禁用：返回错误提示

### 3.6 订单记录功能

#### 3.6.1 订单记录查看
**功能描述：** 用户可以查看自己的订单记录

**功能要求：**
- 显示用户的所有订单记录（包括作为买家和卖家的记录）
- 支持分页显示
- 支持按订单类型筛选（购买/出售）
- 支持按状态筛选（待处理/已接受/已完成/已取消）
- 显示订单信息：商品标题、价格、订单类型、状态、交易对方、时间等
- 需要登录才能查看

**输入：**
- 页码（可选，默认1）
- 每页数量（可选，默认10）
- 订单类型（可选：buy/sell）
- 状态（可选）

**输出：**
- 订单记录列表
- 总记录数
- 总页数
- 当前页码

**异常处理：**
- 未登录：返回401错误

### 3.7 图片上传功能

#### 3.7.1 图片上传
**功能描述：** 用户可以上传商品图片

**功能要求：**
- 支持上传图片文件（png, jpg, jpeg, gif格式）
- 自动生成唯一文件名，避免覆盖
- 图片保存到服务器指定目录（imgs/product）
- 返回图片访问URL
- 支持多文件上传

**输入：**
- 图片文件（必填）

**输出：**
- 图片URL
- 文件名
- 上传状态

**异常处理：**
- 没有选择文件：返回错误提示
- 文件类型不允许：返回错误提示

## 4. 非功能需求

### 4.1 性能需求
- 系统应支持至少100个并发用户
- 页面响应时间应在3秒以内
- 图片加载应支持懒加载，提高页面性能
- 数据库查询应优化，避免N+1查询问题

### 4.2 安全需求
- 用户密码应加密存储（当前版本为简化实现，实际应使用哈希加密）
- 需要实现身份认证机制（当前使用header传递user-id，实际应使用JWT token）
- 防止SQL注入攻击（使用ORM框架）
- 防止XSS攻击（对用户输入进行转义）
- 文件上传需要验证文件类型和大小

### 4.3 可用性需求
- 系统应保证99%的可用性
- 支持主流浏览器（Chrome、Firefox、Safari、Edge）
- 界面应友好、直观，符合用户使用习惯
- 提供清晰的错误提示信息

### 4.4 可维护性需求
- 代码应具有良好的可读性和注释
- 采用模块化设计，便于维护和扩展
- 数据库结构应规范化，避免数据冗余

### 4.5 兼容性需求
- 前端应支持响应式设计，适配不同屏幕尺寸
- 后端API应遵循RESTful设计规范
- 数据库使用MySQL，版本5.7及以上

## 5. 数据库设计

### 5.1 用户表（User）
- id: 用户ID（主键，UUID）
- username: 用户名（唯一，非空）
- password: 密码（非空）
- email: 邮箱（唯一，非空）
- phone: 手机号（可选）
- avatar: 头像URL（可选）
- user_type: 用户类型（student/teacher/admin，默认student）
- is_active: 是否启用（布尔值，默认true）
- created_at: 创建时间

### 5.2 商品表（Product）
- id: 商品ID（主键，UUID）
- title: 商品标题（非空）
- description: 商品描述（非空）
- price: 价格（浮点数，非空）
- category: 分类（非空）
- images: 图片路径（文本，多个路径用逗号分隔）
- status: 状态（available/pending/sold，默认available）
- is_active: 是否启用（布尔值，默认true）
- disable_reason: 禁用理由（可选）
- seller_id: 卖家ID（外键，关联User表）
- views: 浏览量（整数，默认0）
- location: 位置（可选）
- brand: 品牌（可选）
- condition: 新旧程度（new/like_new/good/fair/poor，可选）
- created_at: 创建时间
- updated_at: 更新时间

### 5.3 交易表（Transaction）
- id: 交易ID（主键，UUID）
- product_id: 商品ID（外键，关联Product表）
- buyer_id: 买家ID（外键，关联User表）
- status: 状态（pending/accepted/completed/cancelled，默认pending）
- request_time: 请求时间（默认当前时间）
- accept_time: 接受时间（可选）
- complete_time: 完成时间（可选）
- cancel_time: 取消时间（可选）
- notes: 备注（可选）
- final_price: 最终价格（浮点数，可选）
- cancel_reason: 取消理由（可选）

### 5.4 订单记录表（OrderRecord）
- id: 订单记录ID（主键，UUID）
- transaction_id: 交易ID（外键，关联Transaction表）
- user_id: 用户ID（外键，关联User表）
- order_type: 订单类型（buy/sell，非空）
- product_title: 商品标题（非空）
- product_price: 商品价格（浮点数，非空）
- order_time: 订单时间（默认当前时间）
- order_status: 订单状态（非空）
- other_party_id: 交易对方ID（外键，关联User表）

### 5.5 收藏表（Favorite）
- id: 收藏ID（主键，UUID）
- user_id: 用户ID（外键，关联User表）
- product_id: 商品ID（外键，关联Product表）
- created_at: 创建时间
- 唯一约束：user_id + product_id

### 5.6 联系请求表（ContactRequest）
- id: 联系请求ID（主键，UUID）
- product_id: 商品ID（外键，关联Product表）
- buyer_id: 买家ID（外键，关联User表）
- seller_id: 卖家ID（外键，关联User表）
- message: 留言信息（可选）
- status: 状态（active/archived，默认active）
- created_at: 创建时间
- updated_at: 更新时间
- 唯一约束：product_id + buyer_id

## 6. 数据流描述

### 6.1 用户注册数据流
**数据流编号：** D01  
**数据流名称：** 用户注册信息流  
**简述：** 新用户提交注册信息，系统验证并创建用户账号  
**数据流来源：** 前端注册页面  
**数据流去向：** 用户表（User）  
**数据流组成：** 用户名、密码、邮箱、手机号（可选）、头像（可选）、用户类型  
**数据流量：** 每天约50-200次  
**高峰流量：** 开学季每天约500次  

### 6.2 用户登录数据流
**数据流编号：** D02  
**数据流名称：** 用户登录信息流  
**简述：** 用户提交登录凭证，系统验证并返回用户信息  
**数据流来源：** 前端登录页面  
**数据流去向：** 用户表（User）  
**数据流组成：** 用户名、密码、用户类型  
**数据流量：** 每天约500-2000次  
**高峰流量：** 每天约5000次  

### 6.3 商品发布数据流
**数据流编号：** D03  
**数据流名称：** 商品信息流  
**简述：** 用户提交商品信息，系统验证并保存商品数据  
**数据流来源：** 前端商品发布页面  
**数据流去向：** 商品表（Product）、图片存储目录  
**数据流组成：** 商品标题、描述、价格、分类、图片、位置、品牌、新旧程度、卖家ID  
**数据流量：** 每天约100-500次  
**高峰流量：** 每天约1000次  

### 6.4 商品浏览数据流
**数据流编号：** D04  
**数据流名称：** 商品查询信息流  
**简述：** 用户提交查询条件，系统返回符合条件的商品列表  
**数据流来源：** 前端商品列表页面  
**数据流去向：** 商品表（Product）  
**数据流组成：** 页码、每页数量、分类、搜索关键词、状态、用户ID  
**数据流量：** 每天约5000-20000次  
**高峰流量：** 每天约50000次  

### 6.5 交易创建数据流
**数据流编号：** D05  
**数据流名称：** 交易请求信息流  
**简述：** 买家提交交易请求，系统创建交易记录和订单记录  
**数据流来源：** 前端商品详情页面  
**数据流去向：** 交易表（Transaction）、订单记录表（OrderRecord）、商品表（Product）  
**数据流组成：** 商品ID、买家ID、备注、最终价格  
**数据流量：** 每天约50-200次  
**高峰流量：** 每天约500次  

### 6.6 交易状态更新数据流
**数据流编号：** D06  
**数据流名称：** 交易状态更新信息流  
**简述：** 用户或管理员更新交易状态，系统更新相关记录  
**数据流来源：** 前端交易管理页面  
**数据流去向：** 交易表（Transaction）、订单记录表（OrderRecord）、商品表（Product）  
**数据流组成：** 交易ID、新状态、操作时间、操作者ID  
**数据流量：** 每天约100-400次  
**高峰流量：** 每天约800次  

### 6.7 收藏操作数据流
**数据流编号：** D07  
**数据流名称：** 收藏信息流  
**简述：** 用户添加或取消收藏，系统更新收藏表  
**数据流来源：** 前端商品详情页面或收藏页面  
**数据流去向：** 收藏表（Favorite）  
**数据流组成：** 用户ID、商品ID、操作类型（添加/删除）  
**数据流量：** 每天约200-800次  
**高峰流量：** 每天约2000次  

### 6.8 联系卖家数据流
**数据流编号：** D08  
**数据流名称：** 联系请求信息流  
**简述：** 买家提交联系请求，系统创建或更新联系记录  
**数据流来源：** 前端商品详情页面  
**数据流去向：** 联系请求表（ContactRequest）  
**数据流组成：** 商品ID、买家ID、卖家ID、留言信息  
**数据流量：** 每天约50-200次  
**高峰流量：** 每天约400次  

### 6.9 图片上传数据流
**数据流编号：** D09  
**数据流名称：** 图片文件流  
**简述：** 用户上传商品图片，系统保存文件并返回URL  
**数据流来源：** 前端商品发布/编辑页面  
**数据流去向：** 图片存储目录（imgs/product）  
**数据流组成：** 图片文件、文件名、文件类型  
**数据流量：** 每天约200-1000次  
**高峰流量：** 每天约2000次  

### 6.10 用户管理数据流
**数据流编号：** D10  
**数据流名称：** 用户管理信息流  
**简述：** 管理员查询和管理用户，系统返回用户列表或更新用户状态  
**数据流来源：** 前端用户管理页面  
**数据流去向：** 用户表（User）  
**数据流组成：** 页码、每页数量、搜索关键词、用户类型、用户ID（更新时）、新状态  
**数据流量：** 每天约10-50次  
**高峰流量：** 每天约100次  

### 6.11 商品管理数据流
**数据流编号：** D11  
**数据流名称：** 商品管理信息流  
**简述：** 管理员查询和管理商品，系统返回商品列表或更新商品状态  
**数据流来源：** 前端商品管理页面  
**数据流去向：** 商品表（Product）  
**数据流组成：** 页码、每页数量、搜索关键词、状态、商品ID（更新时）、禁用理由  
**数据流量：** 每天约20-100次  
**高峰流量：** 每天约200次  

## 7. 处理逻辑描述

### 7.1 用户注册处理
**处理逻辑编号：** P01  
**处理逻辑名称：** 用户注册验证与创建  
**简述：** 验证用户注册信息的合法性，创建新用户账号  
**输入的数据流：** D01（用户注册信息流）  
**处理描述：**
1. 接收用户提交的注册信息
2. 验证必填字段（用户名、密码、邮箱）是否为空
3. 检查用户名是否已存在
4. 检查邮箱是否已被注册
5. 验证用户类型（非管理员不能创建管理员账号）
6. 生成用户ID（UUID）
7. 设置默认值（用户类型为student，is_active为true，创建时间为当前时间）
8. 将用户信息保存到用户表
9. 返回注册成功消息和用户ID

**输出的数据流：** 注册结果（成功/失败消息、用户ID或错误信息）  
**处理频率：** 每天约50-200次  

### 7.2 用户登录处理
**处理逻辑编号：** P02  
**处理逻辑名称：** 用户身份验证  
**简述：** 验证用户登录凭证，返回用户信息  
**输入的数据流：** D02（用户登录信息流）  
**处理描述：**
1. 接收用户提交的登录信息（用户名、密码、用户类型）
2. 根据用户名和密码查询用户表
3. 验证用户是否存在
4. 检查用户是否处于活跃状态（is_active）
5. 验证用户类型是否匹配
6. 如果验证通过，返回用户信息（用户ID、用户名、邮箱、用户类型）
7. 如果验证失败，返回错误信息

**输出的数据流：** 登录结果（用户信息或错误信息）  
**处理频率：** 每天约500-2000次  

### 7.3 商品发布处理
**处理逻辑编号：** P03  
**处理逻辑名称：** 商品信息验证与保存  
**简述：** 验证商品信息的完整性，保存商品数据  
**输入的数据流：** D03（商品信息流）  
**处理描述：**
1. 验证用户是否已登录
2. 接收商品信息（标题、描述、价格、分类等）
3. 验证必填字段是否为空
4. 验证价格是否为有效数字
5. 处理图片路径（将多个图片路径用逗号分隔存储）
6. 生成商品ID（UUID）
7. 设置默认值（status为available，is_active为true，views为0，创建时间和更新时间为当前时间）
8. 将商品信息保存到商品表
9. 返回创建成功消息和商品ID

**输出的数据流：** 商品创建结果（成功/失败消息、商品ID或错误信息）  
**处理频率：** 每天约100-500次  

### 7.4 商品查询处理
**处理逻辑编号：** P04  
**处理逻辑名称：** 商品列表查询与筛选  
**简述：** 根据查询条件筛选商品，返回分页结果  
**输入的数据流：** D04（商品查询信息流）  
**处理描述：**
1. 接收查询参数（页码、每页数量、分类、搜索关键词、状态、用户ID等）
2. 构建查询条件
3. 根据用户类型和权限设置可见性过滤（普通用户只能看到可交易且未被禁用的商品）
4. 如果指定了分类，添加分类过滤条件
5. 如果指定了搜索关键词，在标题和描述中搜索
6. 如果指定了状态，添加状态过滤条件
7. 如果指定了卖家ID，添加卖家过滤条件
8. 按创建时间倒序排序
9. 执行分页查询
10. 处理图片数据（将逗号分隔的字符串转换为数组）
11. 返回商品列表、总记录数、总页数、当前页码

**输出的数据流：** 商品列表数据（商品信息数组、分页信息）  
**处理频率：** 每天约5000-20000次  

### 7.5 商品详情处理
**处理逻辑编号：** P05  
**处理逻辑名称：** 商品详情查询与浏览量更新  
**简述：** 查询商品详细信息，更新浏览量  
**输入的数据流：** 商品ID、用户ID  
**处理描述：**
1. 验证用户是否已登录
2. 根据商品ID查询商品信息
3. 验证商品是否存在
4. 检查商品可见性（普通用户不能查看被禁用的商品，除非是自己的）
5. 查询卖家信息
6. 查询相关推荐商品（同分类的其他商品，随机选择3个）
7. 增加商品浏览量（views字段+1）
8. 处理图片数据（将逗号分隔的字符串转换为数组）
9. 返回商品详细信息、卖家信息、推荐商品列表

**输出的数据流：** 商品详情数据（商品信息、卖家信息、推荐商品）  
**处理频率：** 每天约2000-8000次  

### 7.6 交易创建处理
**处理逻辑编号：** P06  
**处理逻辑名称：** 交易请求创建与订单记录生成  
**简述：** 创建交易请求，生成买卖双方的订单记录  
**输入的数据流：** D05（交易请求信息流）  
**处理描述：**
1. 验证用户是否已登录
2. 接收交易信息（商品ID、备注、最终价格）
3. 查询商品信息
4. 验证商品是否存在且可交易
5. 验证商品未被禁用
6. 验证不能购买自己的商品
7. 检查是否已存在待处理的交易请求
8. 生成交易ID（UUID）
9. 创建交易记录（status为pending，request_time为当前时间）
10. 更新商品状态为pending
11. 创建买家订单记录（order_type为buy）
12. 创建卖家订单记录（order_type为sell）
13. 提交事务
14. 返回创建成功消息和交易ID

**输出的数据流：** 交易创建结果（成功/失败消息、交易ID或错误信息）  
**处理频率：** 每天约50-200次  

### 7.7 交易状态更新处理
**处理逻辑编号：** P07  
**处理逻辑名称：** 交易状态变更与相关数据更新  
**简述：** 更新交易状态，同步更新订单记录和商品状态  
**输入的数据流：** D06（交易状态更新信息流）  
**处理描述：**
1. 验证用户是否已登录
2. 接收交易ID和新状态
3. 查询交易信息
4. 验证交易是否存在
5. 验证操作权限（卖家可以接受，交易双方可以完成，买家可以取消）
6. 验证当前状态是否允许变更
7. 更新交易状态
8. 根据新状态更新相应时间字段（accept_time、complete_time、cancel_time）
9. 如果状态为accepted，更新商品状态为pending
10. 如果状态为completed，更新商品状态为sold
11. 如果状态为cancelled且商品状态为pending，恢复商品状态为available
12. 更新相关订单记录的状态
13. 提交事务
14. 返回更新成功消息

**输出的数据流：** 状态更新结果（成功/失败消息）  
**处理频率：** 每天约100-400次  

### 7.8 收藏操作处理
**处理逻辑编号：** P08  
**处理逻辑名称：** 收藏添加与删除  
**简述：** 添加或删除用户对商品的收藏  
**输入的数据流：** D07（收藏信息流）  
**处理描述：**
1. 验证用户是否已登录
2. 接收操作类型（添加/删除）和商品ID
3. 查询商品信息，验证商品是否存在
4. 如果是添加操作：
   - 检查是否已收藏
   - 如果未收藏，创建收藏记录
   - 返回收藏成功消息
5. 如果是删除操作：
   - 查询收藏记录
   - 如果存在，删除收藏记录
   - 返回取消收藏成功消息

**输出的数据流：** 收藏操作结果（成功/失败消息）  
**处理频率：** 每天约200-800次  

### 7.9 联系卖家处理
**处理逻辑编号：** P09  
**处理逻辑名称：** 联系请求创建与更新  
**简述：** 创建或更新买家与卖家的联系记录  
**输入的数据流：** D08（联系请求信息流）  
**处理描述：**
1. 验证用户是否已登录
2. 接收联系信息（商品ID、留言信息）
3. 查询商品信息
4. 验证商品是否存在且未被禁用
5. 验证不能联系自己的商品
6. 验证卖家账号是否处于活跃状态
7. 查询是否已存在联系记录
8. 如果存在，更新联系记录（更新message和updated_at）
9. 如果不存在，创建新的联系记录
10. 查询卖家信息
11. 返回联系成功消息、卖家信息、商品信息

**输出的数据流：** 联系结果（成功/失败消息、卖家信息、商品信息）  
**处理频率：** 每天约50-200次  

### 7.10 图片上传处理
**处理逻辑编号：** P10  
**处理逻辑名称：** 图片文件验证与存储  
**简述：** 验证图片文件，保存到服务器并返回访问URL  
**输入的数据流：** D09（图片文件流）  
**处理描述：**
1. 接收上传的图片文件
2. 验证文件是否存在
3. 验证文件类型（png、jpg、jpeg、gif）
4. 生成安全的文件名（使用secure_filename）
5. 生成唯一文件名（UUID + 原文件扩展名）
6. 保存文件到指定目录（imgs/product）
7. 生成文件访问URL（/imgs/product/文件名）
8. 返回文件URL、文件名、上传状态

**输出的数据流：** 上传结果（文件URL、文件名、状态或错误信息）  
**处理频率：** 每天约200-1000次  

### 7.11 用户管理处理
**处理逻辑编号：** P11  
**处理逻辑名称：** 用户列表查询与状态管理  
**简述：** 管理员查询用户列表，启用或禁用用户  
**输入的数据流：** D10（用户管理信息流）  
**处理描述：**
1. 验证用户是否已登录且为管理员
2. 接收查询参数（页码、每页数量、搜索关键词、用户类型）
3. 构建查询条件
4. 如果指定了搜索关键词，在用户名和邮箱中搜索
5. 如果指定了用户类型，添加类型过滤条件
6. 按创建时间倒序排序
7. 执行分页查询
8. 返回用户列表、总记录数、总页数、当前页码
9. 如果是状态更新操作：
   - 验证不能禁用自己的账号
   - 验证不能禁用其他管理员账号
   - 更新用户is_active状态
   - 返回更新成功消息

**输出的数据流：** 用户列表数据或状态更新结果  
**处理频率：** 每天约10-50次  

### 7.12 商品管理处理
**处理逻辑编号：** P12  
**处理逻辑名称：** 商品列表查询与状态管理  
**简述：** 管理员查询商品列表，禁用或启用商品  
**输入的数据流：** D11（商品管理信息流）  
**处理描述：**
1. 验证用户是否已登录且为管理员
2. 接收查询参数（页码、每页数量、搜索关键词、状态）
3. 构建查询条件（管理员可以查看所有商品，包括被禁用的）
4. 执行分页查询
5. 返回商品列表、总记录数、总页数、当前页码
6. 如果是状态更新操作：
   - 查询商品信息
   - 如果是禁用操作，验证必须提供禁用理由
   - 更新商品is_active状态和disable_reason
   - 返回更新成功消息

**输出的数据流：** 商品列表数据或状态更新结果  
**处理频率：** 每天约20-100次  

## 8. 数据存储描述

### 8.1 用户数据存储
**数据存储编号：** F01  
**数据存储名称：** 用户表（User）  
**简述：** 存储系统中所有用户的基本信息和状态  
**数据储存组成：** 用户id、用户名、密码、邮箱、电话号码、创建时间、头像url、合法状态、用户类型  
**关键字：** 用户id（主键）、用户名（唯一索引）、邮箱（唯一索引）  
**相关联的处理：** P01（用户注册处理）、P02（用户登录处理）、P11（用户管理处理）  

### 8.2 商品数据存储
**数据存储编号：** F02  
**数据存储名称：** 商品表（Product）  
**简述：** 存储系统中所有商品的信息和状态  
**数据储存组成：** 商品id、商品title、商品描述、商品价格、商品种类、商品图片、商品状态、创建时间、更新时间、卖方id、浏览量、地点、品牌、新旧程度、合法状态、禁用理由  
**关键字：** 商品id（主键）、卖方id（外键，关联用户表）  
**相关联的处理：** P03（商品发布处理）、P04（商品查询处理）、P05（商品详情处理）、P06（交易创建处理）、P07（交易状态更新处理）、P12（商品管理处理）  

### 8.3 交易数据存储
**数据存储编号：** F03  
**数据存储名称：** 交易表（Transaction）  
**简述：** 存储系统中所有交易请求的完整信息  
**数据储存组成：** 交易id、商品id、买方id、交易状态、请求时间、接受时间、完成时间、最终价格、备注、取消时间、取消理由  
**关键字：** 交易id（主键）、商品id（外键，关联商品表）、买方id（外键，关联用户表）  
**相关联的处理：** P06（交易创建处理）、P07（交易状态更新处理）  

### 8.4 订单记录数据存储
**数据存储编号：** F04  
**数据存储名称：** 订单记录表（OrderRecord）  
**简述：** 存储每次交易请求产生的买卖双方订单记录  
**数据储存组成：** 交易记录id、交易id、交易操作方id、操作类型、商品标题、商品价格、订单时间、订单状态、交易对方id  
**关键字：** 交易记录id（主键）、交易id（外键，关联交易表）、交易操作方id（外键，关联用户表）、交易对方id（外键，关联用户表）  
**相关联的处理：** P06（交易创建处理）、P07（交易状态更新处理）  

### 8.5 收藏数据存储
**数据存储编号：** F05  
**数据存储名称：** 收藏表（Favorite）  
**简述：** 存储用户对商品的收藏关系  
**数据储存组成：** 数据id、用户id、商品id、创建时间  
**关键字：** 数据id（主键）、用户id（外键，关联用户表）、商品id（外键，关联商品表）、（用户id，商品id）唯一约束  
**相关联的处理：** P08（收藏操作处理）  

### 8.6 联系请求数据存储
**数据存储编号：** F06  
**数据存储名称：** 联系请求表（ContactRequest）  
**简述：** 存储买家与卖家的联系记录  
**数据储存组成：** 联系id、商品id、买家id、卖方id、消息、创建时间、更新时间、状态  
**关键字：** 联系id（主键）、商品id（外键，关联商品表）、买家id（外键，关联用户表）、卖方id（外键，关联用户表）、（商品id，买家id）唯一约束  
**相关联的处理：** P09（联系卖家处理）  

### 8.7 图片文件存储
**数据存储编号：** F07  
**数据存储名称：** 商品图片目录（imgs/product）  
**简述：** 存储用户上传的商品图片文件  
**数据储存组成：** 图片文件（以唯一文件名存储）  
**关键字：** 文件名（唯一）  
**相关联的处理：** P03（商品发布处理）、P10（图片上传处理）  

## 9. 接口设计

### 9.1 用户管理接口
- POST /api/users/register - 用户注册
- POST /api/users/login - 用户登录
- GET /api/users/profile - 获取用户信息
- PUT /api/users/profile - 更新用户信息
- GET /api/users - 获取用户列表（管理员）
- PUT /api/users/{user_id}/toggle - 切换用户状态（管理员）

### 9.2 商品管理接口
- POST /api/products - 创建商品
- GET /api/products - 获取商品列表
- GET /api/products/{product_id} - 获取商品详情
- PUT /api/products/{product_id} - 更新商品
- DELETE /api/products/{product_id} - 删除商品
- PUT /api/products/{product_id}/toggle - 禁用/启用商品（管理员）

### 9.3 交易管理接口
- POST /api/transactions - 创建交易请求
- GET /api/transactions - 获取交易列表
- GET /api/transactions/{transaction_id} - 获取交易详情
- PUT /api/transactions/{transaction_id}/accept - 接受交易
- PUT /api/transactions/{transaction_id}/complete - 完成交易
- PUT /api/transactions/{transaction_id}/cancel - 取消交易

### 9.4 收藏接口
- POST /api/favorites - 添加收藏
- DELETE /api/favorites/{product_id} - 取消收藏
- GET /api/favorites - 获取收藏列表
- GET /api/favorites/check/{product_id} - 检查收藏状态

### 9.5 其他接口
- POST /api/contact-requests - 联系卖家
- GET /api/order-records - 获取订单记录
- POST /api/upload - 上传图片
- GET /imgs/product/{filename} - 获取商品图片

## 10. 前端页面设计

### 10.1 公开页面
- 首页（Home）：展示轮播图、搜索栏、统计信息、热门分类、推荐内容
- 登录页（Login）：用户登录界面
- 注册页（Register）：用户注册界面
- 商品列表页（ProductList）：商品浏览、搜索、筛选
- 商品详情页（ProductDetail）：商品详细信息展示

### 10.2 用户页面（需要登录）
- 个人中心（Profile）：个人信息查看和编辑
- 我的收藏（Favorites）：收藏商品列表
- 发布商品（CreateProduct）：商品发布表单
- 编辑商品（EditProduct）：商品编辑表单
- 交易列表（TransactionList）：交易记录列表
- 交易详情（TransactionDetail）：交易详细信息

### 10.3 管理员页面（需要管理员权限）
- 用户管理（UserManagement）：用户列表、搜索、筛选、启用/禁用
- 商品管理（ProductManagement）：商品列表、搜索、筛选、禁用/启用

## 11. 技术架构

### 11.1 前端技术栈
- React：前端框架
- React Router：路由管理
- Ant Design：UI组件库
- Axios：HTTP请求库
- Styled Components：样式管理

### 11.2 后端技术栈
- Flask：Web框架
- SQLAlchemy：ORM框架
- Flask-Migrate：数据库迁移工具
- Flask-CORS：跨域支持
- PyMySQL：MySQL数据库驱动

### 11.3 数据库
- MySQL：关系型数据库

## 12. 开发规范

### 12.1 代码规范
- 遵循PEP 8（Python）和ESLint（JavaScript）代码规范
- 使用有意义的变量和函数命名
- 添加必要的注释说明
- 保持代码结构清晰，模块化设计

### 12.2 API设计规范
- 遵循RESTful API设计原则
- 使用统一的响应格式
- 提供清晰的错误信息
- 使用合适的HTTP状态码

### 12.3 数据库设计规范
- 使用UUID作为主键
- 添加必要的索引
- 使用外键约束保证数据完整性
- 添加时间戳字段（created_at, updated_at）

## 13. 测试需求

### 13.1 功能测试
- 用户注册、登录功能测试
- 商品发布、编辑、删除功能测试
- 交易流程测试
- 收藏功能测试
- 管理员功能测试

### 13.2 性能测试
- 并发用户测试
- 响应时间测试
- 数据库查询性能测试

### 13.3 安全测试
- 身份认证测试
- 权限控制测试
- SQL注入防护测试
- XSS攻击防护测试

## 14. 部署需求

### 14.1 环境要求
- Python 3.7+
- Node.js 14+
- MySQL 5.7+
- 服务器内存：至少2GB
- 服务器存储：至少10GB

### 14.2 部署步骤
1. 安装Python依赖：pip install -r requirements.txt
2. 安装前端依赖：npm install
3. 配置数据库连接
4. 运行数据库迁移
5. 启动后端服务：python app.py
6. 构建前端：npm run build
7. 配置Web服务器（如Nginx）代理前端静态文件和后端API

## 15. 后续优化建议

### 15.1 功能优化
- 实现消息通知系统
- 添加商品评价功能
- 实现站内信功能
- 添加商品举报功能
- 实现商品推荐算法

### 15.2 技术优化
- 实现JWT身份认证
- 添加Redis缓存
- 实现图片CDN加速
- 添加日志系统
- 实现数据备份机制

### 15.3 安全优化
- 实现密码加密存储
- 添加验证码功能
- 实现API限流
- 添加操作日志记录

## 16. 附录

### 16.1 术语表
- **商品状态**：available（可交易）、pending（交易中）、sold（已售出）
- **交易状态**：pending（待处理）、accepted（已接受）、completed（已完成）、cancelled（已取消）
- **用户类型**：student（学生）、teacher（教师）、admin（管理员）
- **订单类型**：buy（购买）、sell（出售）

### 16.2 参考资料
- Flask官方文档
- React官方文档
- Ant Design组件库文档
- SQLAlchemy文档

---

**文档版本：** v1.0  
**编写日期：** 2024年  
**最后更新：** 2024年

